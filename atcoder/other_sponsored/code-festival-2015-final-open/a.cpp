#include <bits/stdc++.h>

#define rep(i,n) for(int i=0;i<(n);i++)

using namespace std;

int main(){
	string a,b,c; cin>>a>>b>>c;
	puts(a.length()==5&&b.length()==7&&c.length()==5?"valid":"invalid");
	return 0;
}
