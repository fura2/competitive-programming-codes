#include <bits/stdc++.h>

#define rep(i,n) for(int i=0;i<(n);i++)

using namespace std;

int main(){
	rep(i,1000){
		printf("%03d\n",i);
		fflush(stdout);
		char s[9]; scanf("%s",s);
		if(s[0]=='u') break;
	}
	return 0;
}
