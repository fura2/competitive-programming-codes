#include <bits/stdc++.h>

#define rep(i,n) for(int i=0;i<(n);i++)

using namespace std;

const double PI=acos(-1);

int main(){
	double a,b; scanf("%lf%lf",&a,&b);
	printf("%.9f\n",2*PI*(b-a));
	return 0;
}
