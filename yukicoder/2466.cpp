#include "template.hpp"

int main() {
    int n;
    cin >> n;
    // rep (k, 1, 20) {
    //     double x = sqrt(1);
    //     rrep (i, k - 1) {
    //         x = sqrt(1 + (n + i) * x);
    //     }
    //     cout << k << ": " << x << endl;
    // }
    cout << n + 1 << endl;
    return 0;
}
