#include <bits/stdc++.h>

#define rep(i,n) for(int i=0;i<(n);i++)

using namespace std;

int main(){
	while(1){
		int a; scanf("%d",&a);
		if(a==0) break;
		printf("%c\n","yukicoder"[a-1]);
	}
	return 0;
}
